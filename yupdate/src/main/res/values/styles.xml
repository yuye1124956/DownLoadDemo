<resources>

    <style name="translucent" parent="android:Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name="android:windowIsFloating">true</item>
        <!--背景是否模糊显示，为false时效果为全透明-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="updateDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item> <!-- 隐藏标题 -->
        <item name="android:backgroundDimEnabled">true</item> <!-- 设置弹出框弹出的背景 模糊 -->
        <item name="android:windowFullscreen">true</item> <!-- 全屏显示 -->
    </style>
</resources>